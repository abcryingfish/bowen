# 板块研究领域

## 分类与评分适配

**评分适配类型（Analysis Archetype）**：每个研究实体唯一的语义评分适配类型，决定指标适用性、子指标权重、阈值和横截面比较组；不改变六维定义和顶层权重。

**分类属性（Classification Facets）**：可多值的标准化语义属性集合，用于检索、解释、证据关联和知识图谱；属性可以重叠，但不能替代唯一评分适配类型。

**类型复核状态（Type Review Status）**：表示自动分类是否达到正式发布门槛；`review_required`时允许客观维度发布，但暂停政策、基本面、估值和综合评分；这不是程序失败。

本领域描述板块及未来个股研究中的实体身份、时间状态、证据、评估和预测。这里的术语是数据表、API、前端和知识图谱共同使用的唯一语言。

## 实体身份

**研究实体（Entity）**：
系统能够独立研究、评分和追踪的对象；当前为板块，未来可扩展为个股。
_避免：对象、标的（未说明具体含义时）_

**实体ID（entity_id）**：
系统内部生成且永久不变的身份标识，不包含同花顺代码、名称或其他供应商语义。
_避免：板块代码、同花顺代码、source_code_

**来源映射（Source Mapping）**：
研究实体在特定外部来源、分类体系和有效期内对应的代码与名称。
_避免：实体身份、永久代码_

**规范名称（canonical_name）**：
系统当前用于展示和检索的首选名称；名称变化不改变实体ID。
_避免：唯一名称、永久名称_

**实体版本（entity_version）**：
实体元数据发生可追踪变化后的递增版本号，不代表创建了新的研究实体。
_避免：评分版本、来源代码版本_

**生命周期事件（Lifecycle Event）**：
研究实体的创建、改名、代码变化、停用、恢复、合并或拆分记录。
_避免：每日成分快照、普通字段更新_

## 成分关系

**成分快照（Constituent Snapshot）**：
一次成功采集中观察到的全部板块与成分股关系；即使与上次完全相同也形成独立快照。
_避免：成分变化、当前成分表_

**来源成分快照（Raw Constituent Snapshot）**：
保留来源返回原貌的完整成分快照，包含北交所成分和来源字段，仅用于审计与派生。
_避免：分析股票池、可分析快照_

**可分析成分快照（Eligible Constituent Snapshot）**：
由来源成分快照按已版本化的资格规则生成、只包含允许参与研究计算的沪深A股关系。
_避免：原始快照、全市场快照_

**成分变化（Membership Change）**：
相邻两个已发布完整快照之间检测到的新增或移除关系，不代表来源官方公布的精确生效时间。
_避免：官方调入公告、生命周期事件_

**观察区间（Observed Interval）**：
某一成分关系在连续完整快照中被系统观察到的时间范围。
_避免：官方有效期、交易所生效区间_

## 市场序列

**市场序列（Market Series）**：
可按交易日连续查询的价格或基准序列，由独立 `series_id` 标识并映射到研究实体。
_避免：研究实体、板块身份_

**来源指数（Source Index）**：
外部来源直接提供的板块指数序列，是板块历史行情的唯一主序列。
_避免：成分合成指数、缺口填充值_

**合成指数（Synthetic Index）**：
按已版本化权重方法从成分股行情计算的辅助序列，不得替代或补写来源指数。
_避免：官方指数、来源指数_

**基准序列（Benchmark Series）**：
用于计算相对收益、Beta和相对回撤的对照市场序列，其映射具有有效期。
_避免：板块主指数_

**市场观察（Market Observation）**：
某次研究批次从指定市场序列和数据截止日得到的可复现行情状态与质量摘要。
_避免：原始日线、评分结论_

**市场特征（Market Feature）**：
使用明确输入序列、窗口和算法版本计算的趋势、量价、波动或回撤数值，本身不等于任何评分维度。
_避免：技术评分、风险评分、模型结论_

**历史较短（Short History）**：
市场序列在来源中确实尚未积累足够交易日，不等同于应有数据缺失。
_避免：数据缺口、抓取失败_

**数据缺口（Data Gap）**：
在基准正常交易且序列按规则应有记录时缺失的交易日。
_避免：新指数历史较短_

## 评分

**规则分（Rule Score）**：
由版本化指标、阈值、权重和证据规则确定性计算的维度基础分。
_避免：模型主观分、最终维度分_

**模型调整（Model Adjustment）**：
模型基于可引用新增证据对规则分作出的受限增减值，必须独立保存而不能混入规则分。
_避免：规则分、人工修订_

**最终维度分（Final Dimension Score）**：
规则分与合规模型调整相加并限制在0至10后的正式维度评分。
_避免：置信度、综合分_

**评分置信度（Score Confidence）**：
数据覆盖、时效、证据质量和冲突程度形成的独立可靠性数值，不是评分高低。
_避免：最终维度分、上涨概率_

**评分策略版本（Scoring Policy Version）**：
唯一确定指标、阈值、权重、缺失处理和模型调整边界的一套正式规则标识。
_避免：提示词版本、数据模式版本_

**评估修订（Assessment Revision）**：
同一研究实体和研究日期因数据修正或重新运行产生的后续正式记录；旧记录保留但不再作为当前展示版本。
_避免：覆盖旧评分、历史回填_

**历史回填（Historical Backfill）**：
为系统正式启用日前的历史日期重新生成市场特征、评分或预测。
_避免：读取历史日线作为最新指标窗口_

## 评分维度

**政策维度（Policy Dimension）**：
政策权威层级、支持或约束方向、覆盖范围和执行进度形成的净支持程度。
_避免：政策新闻数量、基本面兑现_

**基本面维度（Fundamental Dimension）**：
盈利、收入、经营质量、供需和产业价格所反映的经营改善程度。
_避免：估值高低、股价趋势_

**资金维度（Capital Dimension）**：
由可验证成交、换手、融资、ETF份额和机构持仓等数据反映的资金参与强度与方向。
_避免：无来源的主力资金、价格趋势_

**技术维度（Technical Dimension）**：
指数及成分股价格的趋势、动量、相对强弱和价格广度。
_避免：波动率、最大回撤、成交资金强度_

**估值维度（Valuation Dimension）**：
估值倍数、盈利收益率及横截面可比结果反映的估值吸引力。
_避免：盈利增长、价格动量_

**风险压力维度（Risk Pressure Dimension）**：
波动、回撤、集中度、可验证拥挤度和重大下行事件形成的风险压力；数值越高表示越危险。
_避免：数据质量、证据冲突、低置信度_

**指标归属（Indicator Ownership）**：
一个正式评分指标唯一所属的评分维度，用于防止相同含义被跨维度重复计分。
_避免：原始字段来源、页面展示栏目_

**绝对阈值分（Absolute Threshold Score）**：
将指标原始值按版本化固定阈值映射到0至10的可跨批次比较分数。
_避免：横截面排名、模型调整_

**横截面分（Cross-sectional Score）**：
将同一研究日期、可比同组板块中的指标百分位映射到0至10的相对分数。
_避免：历史分位、绝对阈值分_

**同组板块（Peer Group）**：
同一分类层级和板块类型下参与横截面比较的有效研究实体集合。
_避免：板块成分股、历史样本_

**可验证拥挤度（Verifiable Crowding）**：
由有明确来源和公式的换手、融资、ETF份额或机构持仓数据形成的拥挤代理。
_避免：无来源的交易拥挤度、主力资金猜测_

**重大下行事件（Material Downside Event）**：
可能显著损害板块供需、盈利、交易资格或风险偏好的已发生事件，按证据确认的严重度评分。
_避免：一般负面观点、重复转载数量_

**资金参与代理（Capital Participation Proxy）**：
由成交额、换手和量价广度推断的参与强度与方向，不证明资金主体或真实净流入。
_避免：主力资金、机构净流入、真实资金流_

**真实资金证据（Direct Capital Evidence）**：
能够识别资金载体、余额或持仓变化的可核验数据，例如融资余额、ETF份额或机构持仓。
_避免：成交额放大、换手率上升_

**估值吸引力（Valuation Attractiveness）**：
在同类板块和明确数据覆盖下，价格相对盈利、净资产及亏损比例所反映的便宜程度；高分表示更有吸引力。
_避免：绝对低价、盈利增长、技术强弱_

**同报告期同比（Same-Period YoY）**：
将同一证券最新已披露报告期与上一年度相同报告期的财务指标比较，避免报告期错配。
_避免：任意两条财务记录相除、公告日同比_

**财务时点约束（Financial Point-in-Time Constraint）**：
只有公告时间不晚于研究日期的财务数据才可进入该日期的基本面计算。
_避免：文件最新值、报告期日期代替公告日期_

## 证据与知识

**证据记录（Evidence Record）**：
带来源、发布时间、抓取时间、内容哈希和质量属性的不可变外部信息观察。
_避免：模型观点、主张、搜索结果列表_

**证据内容（Evidence Content）**：
按内容哈希去重保存的UTF-8正文或抽取文本，与来源元数据通过`content_hash`关联。
_避免：摘要、网页标题_

**来源等级（Source Tier）**：
按权威性、直接性和可核验性划分的证据来源层级。
_避免：搜索排序、模型置信度_

**原子主张（Atomic Claim）**：
只表达一个主体、一个关系、一个客体和明确时间范围的可引用判断。
_避免：多句分析、综合结论、未经证据支持的口号_

**主张状态（Claim Status）**：
主张当前为有效、削弱、失效、过期或待复核的状态，历史变化只追加事件。
_避免：删除旧主张、覆盖历史状态_

**证据冲突（Evidence Conflict）**：
针对同一主体、关系和时间范围的证据支持相反方向主张的状态。
_避免：来源数量少、模型不确定_

**政策信号（Policy Signal）**：
由政策方向、权威性、执行进度和对板块传导强度共同形成的有方向支持程度。
_避免：政策新闻数量、政策热度_

**维度权重覆盖（Dimension Weight Coverage）**：
本次评估中实际有效维度的配置权重之和，用于判断综合分是否可以发布。
_避免：数据行覆盖率、置信度_

**部分评估（Partial Assessment）**：
满足最低维度权重和必需维度要求、但仍有非必需维度缺失的正式综合评估。
_避免：完整评估、用中性分填补后的评估_

**预测（Forecast）**：
针对一个未来交易期限、在特定研究时点不可修改的方向、收益区间和概率判断。
_避免：目标价、事后结果_

**预测结果（Forecast Outcome）**：
预测期限到期后根据同一主序列和基准追加的实际收益与命中评价。
_避免：修改原预测、重新预测_

**预测校准状态（Forecast Calibration Status）**：
表示概率来自先验规则、初步校准还是正式历史校准的版本状态。
_避免：预测置信度、方向结论_

**失效条件（Invalidation Condition）**：
会使预测不再适用的可观测市场、成分或证据条件，写入预测时不可事后修改。
_避免：事后解释、止损建议_

## 运行与发布

**全局原子快照（Atomic Global Snapshot）**：
同一次板块清单及全部成分关系只有在整批验证成功后才共同成为当前正式版本。
_避免：部分新成分与旧成分拼接_

**板块研究任务（Entity Research Task）**：
在固定输入快照、研究日期和策略版本下，对单个研究实体生成特征、评分和预测的幂等工作单元。
_避免：全市场运行批次_

**模型主导单板块研究（Model-led Single-Entity Research）**：
一个Codex任务只研究一个板块，由大语言模型逐轮提出查询、阅读来源、判断相关性、形成主张并完成评分；确定性程序只提供计算、抓取、存储和硬校验。
_避免：规则批量映射、Python关键词研究_

**确定性研究支持程序（Deterministic Research Support）**：
不作语义判断，只执行行情与财务计算、模型指定资料下载、去重、日期截断、存储、契约验证和任务状态管理的程序。
_避免：研究模型、证据相关性判断器_

**证据类别状态（Evidence Category Status）**：
一次单板块研究对某类来源的检索结论，区分有效、搜索后无有效证据、检索失败、不适用、冲突、错配和分析日后排除。
_避免：证据数量、维度评分_

**可重试失败（Retryable Failure）**：
网络、限流、超时、临时服务异常或可修复输出格式错误造成的暂时失败。
_避免：数据质量阻断、业务规则不满足_

**终止失败（Terminal Failure）**：
在当前输入和规则下重复执行不会改善的映射、模式、数据质量或配置错误。
_避免：临时网络错误、低置信度结果_

**过期沿用结果（Stale Carried-forward Result）**：
本轮板块研究失败时继续展示的上一份正式结果，必须标明原分析日期和失败原因。
_避免：本轮成功结果、静默沿用_

**当前指针（Current Pointer）**：
指向某个实体最后一份正式发布结果或全局快照的可重建读取引用，不是历史事实本身。
_避免：最新文件修改时间、历史记录覆盖_

## 时间

**交易日（trade_date）**：
行情或市场指标所属的交易日期。
_避免：抓取日期、研究日期_

**发布日期（published_at）**：
公告、政策、新闻或研报首次对外发布的时间。
_避免：抓取时间_

**抓取时间（fetched_at）**：
系统实际取得外部数据或证据的时间。
_避免：发布时间、生效时间_

**观察时间（observed_at）**：
系统完成一次板块清单及全部成分采集的时间，也是成分快照的时间锚点。
_避免：来源官方生效时间_

**研究日期（analysis_date）**：
一次研究结论所针对的市场日期，不等同于程序实际运行时间。
_避免：run_started_at_

**有效期（valid_from / valid_to）**：
来源映射或状态在现实业务中成立的时间区间；`valid_to` 为空表示当前仍有效。
_避免：文件写入时间_
