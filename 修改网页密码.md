# 修改网页密码

## 用户

这里的密码是多久有效？然后如果我要更改密码的话，如何更改？

## Codex

当前使用的是 Nginx Basic Auth，**密码本身不会自动过期**。只要密码文件存在且未修改，就一直有效。

浏览器通常会临时记住登录信息，关闭所有浏览器窗口或无痕窗口后，下一次访问一般会重新要求输入。具体缓存时间由浏览器决定。

修改密码时，在腾讯云服务器运行：

```bash
htpasswd -B /www/wwwroot/.bowenquant.htpasswd bowen
```

然后输入两次新密码即可。注意不要加 `-c`，否则可能覆盖整个密码文件。

修改后：

- 不需要重启 Nginx。
- 新密码立即生效。
- 浏览器可能仍发送旧密码，随后出现 `401` 并重新弹出登录框。
- 如果未弹窗，关闭所有该网站标签页，使用无痕窗口重新打开。

如果需要给不同的人独立账号，可以添加新用户：

```bash
htpasswd -B /www/wwwroot/.bowenquant.htpasswd 用户名
```

删除某个用户：

```bash
htpasswd -D /www/wwwroot/.bowenquant.htpasswd 用户名
```

Basic Auth 不支持“7天后自动失效”。若以后需要验证码、账号有效期或登录审计，应改用更完整的身份验证方案。现在这种方式适合少量固定人员使用。
